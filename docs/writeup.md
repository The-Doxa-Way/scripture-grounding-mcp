# The Model Never Quotes From Memory

**An open MCP server that makes any AI assistant quote Scripture accurately — built on the YouVersion Platform and Gloo AI Studio.**


## The frontier nobody named

Billions of people now spend hours a day inside AI conversations — ChatGPT, Claude — where Scripture is quoted, paraphrased, and misquoted daily. YouVersion's founder said publicly that leading AI models misquote Scripture 15–60% of the time.

People misquote Scripture because they quote from memory instead of opening the Book. Models fail the same way — statistical memory instead of the page. And the remedy hasn't changed in two thousand years: **read the passage, then quote it**. So we built a Model Context Protocol server where **the model never quotes from memory** — it reads the passage first, like a careful reader.

## What it does

MCP is the open standard plugging tools into nearly every major AI surface. Connect this server and any assistant gains:

- **`get_passage`** — canonical text, verse to whole book ("read me Romans"), via the YouVersion Platform API (BSB by default; 1,400+ versions with your own key).
- **`verify_quote`** — hand it any quote + claimed reference: it fetches canonical text, diffs it word-by-word (whole books verify chapter-by-chapter), and returns a verdict (exact / minor variance / misquote / misattribution / different_translation) with the correction — code, not a model grading itself.
- **`grounded_reply`** — encouragement for a real moment ("I'm anxious about my future"), generated by Gloo AI Studio under one hard rule: retrieve first, quote only the retrieved text, cite it.
- **`verify_register`** — checks any AI reply for first-person, reassurance-empathy, or companion/personhood language via a deterministic rule table; `grounded_reply` runs it on itself, always-on.

Works out of the box, no keys, on the public-domain Berean Standard Bible; add your own YouVersion key for the licensed library. Open source (MIT) — every frontier this challenge names, games to wearables to social, can build on it.

## Measured, not solved

We benchmarked the same Gloo AI Studio call with and without grounding across 34 passages in five genres: exact-quote rate 67.6% ungrounded vs 100% grounded (grounded+verify also 100%); misattribution 0% in both. We also checked our scoring for hidden bias: none of the 11 ungrounded misses were accurate quotes of another public-domain translation (WEB/KJV) — the gap is real. Methodology, corpus, and runner are in the repo — reproducible with free challenge keys.

Named limitations, because trust is the product: misattribution search is corpus-bounded; long-passage alignment is a disclosed heuristic; grounded generation can still err in the words *around* a quote; the register guard is deterministic regex, not semantic understanding. Grounding makes errors detectable, not plausible-looking: not solved, but measurable and smaller.

## Why us, and what's next

We build Doxa, the prophetic encouragement app. Scripture fidelity amid AI's real risks is close to our heart; this server is that conviction, open-sourced. Every line was written new inside the challenge window (the git history shows it): we brought conviction and lessons from our own safety evals, not code. The vision is the brief's own: Scripture present wherever people already are, word-for-word true, in over a thousand languages.

The public backlog lives in docs/ROADMAP.md.

The bridge is built. It's open. Connect it.

---
*Repo: github.com/The-Doxa-Way/scripture-grounding-mcp · Demo video + notebook attached · Built with the YouVersion Platform API and Gloo AI Studio API per the challenge brief.*
